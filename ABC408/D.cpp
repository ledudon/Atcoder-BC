#include <bits/stdc++.h>
#define rep(i, a, b) for(int i = a; i < b; i++)
#define all(x) (x).begin(),(x).end()
typedef long long ll;
using namespace std;

int main(void){
    int t;
    cin >> t;
    for(int i = 0; i < t; i++){
        int n;
        string s;
        cin >> n >> s;
        int length = s.size();
        int j = 0;
        while(j < length){
            if(s[j] == '1'){
                
            }else{
                j++;
            }
        }
    }
    
    return 0;
}