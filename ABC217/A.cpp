#include <bits/stdc++.h>
#define rep(i, a, b) for(int i = a; i < b; i++)
#define all(x) (x).begin(),(x).end()
using namespace std;

int main(void){
    string s, t;
    cin >> s >> t;
    if(s.compare(t) < 0) cout << "Yes" << endl;
    else cout << "No" << endl;
    
    
    return 0;
}